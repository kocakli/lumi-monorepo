<?xml version="1.0" encoding="utf-8"?>
<!-- Lumi Android manifest. Customised from Skip's generated template to add
     POST_NOTIFICATIONS, Firebase Cloud Messaging service registration, and
     a default notification channel id matching the channel created at
     runtime in MainActivity (see "lumi.message"). -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet for Firebase calls / image fetches. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Required to display notifications on Android 13+ (Tiramisu / API 33). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Vibration during notification (haptic feedback for incoming pair messages). -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:label="${PRODUCT_NAME}"
        android:name=".AndroidAppMain"
        android:supportsRtl="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|mnc|colorMode|density|fontScale|fontWeightAdjustment|keyboard|layoutDirection|locale|mcc|navigation|smallestScreenSize|touchscreen|uiMode"
            android:theme="@style/Theme.Lumi.Splash"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Firebase Cloud Messaging — Skip-provided default service handles
             token refresh + message delivery. App-specific routing happens in
             MainActivity once a notification is tapped (deep link → router). -->
        <service
            android:name="skip.firebase.messaging.MessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- When a backend FCM message has no explicit channel_id, Android
             routes it to this channel. We create the matching channel
             ("lumi.message") at runtime with the Lumi-branded custom sound,
             matching the iOS APNs payload's "lumi.message" category. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="lumi.message" />

        <!-- Home-screen widget (Glance, Compose-based). Mirrors the iOS
             LumiWidget — hourly rotating positivity message. -->
        <receiver
            android:name=".LumiGlanceWidgetReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/lumi_widget_info" />
        </receiver>

    </application>
</manifest>
